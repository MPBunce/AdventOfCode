#include <iostream>
#include <fstream>
#include <string>

void solve_day_two(std::ifstream& file) {
    std::string line;
    while (std::getline(file, line)) {
        std::cout << line << std::endl;
    }
}
